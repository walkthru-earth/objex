diff --git a/dist/cog-layer.js b/dist/cog-layer.js
index 5c3deb11ec6cc27e14c988db4d0b63a98108f80e..38d847595385e079ec7c33e065d0689f67a28707 100644
--- a/dist/cog-layer.js
+++ b/dist/cog-layer.js
@@ -49,12 +49,16 @@ export class COGLayer extends RasterTileLayer {
             : parseWkt(crs);
         // @ts-expect-error - proj4 typings are incomplete and don't support
         // wkt-parser input
-        const converter4326 = proj4(sourceProjection, "EPSG:4326");
+        // objex patch: +over disables proj4 adjust_lon to fix antimeridian
+        // wrapping. Without it, forward(-180.001 deg) wraps to positive easting,
+        // causing 360 deg round-trip errors and unconvergeable mesh refinement.
+        // See https://github.com/developmentseed/deck.gl-raster/issues/366
+        const converter4326 = proj4(sourceProjection, "+proj=longlat +datum=WGS84 +over");
         const projectTo4326 = (x, y) => converter4326.forward([x, y], false);
         const projectFrom4326 = (x, y) => converter4326.inverse([x, y], false);
         // @ts-expect-error - proj4 typings are incomplete and don't support
         // wkt-parser input
-        const converter3857 = proj4(sourceProjection, "EPSG:3857");
+        const converter3857 = proj4(sourceProjection, "+proj=merc +a=6378137 +b=6378137 +lat_ts=0 +lon_0=0 +x_0=0 +y_0=0 +k=1 +units=m +nadgrids=@null +over");
         const projectTo3857 = makeClampedForwardTo3857((x, y) => converter3857.forward([x, y], false), projectTo4326);
         const projectFrom3857 = (x, y) => converter3857.inverse([x, y], false);
         const units = sourceProjection.units;
diff --git a/dist/index.d.ts b/dist/index.d.ts
index b698dc7d839f9290b72e01144c689d8a3849f13a..99949a184313560a6c1b6bbb95aadf78c3e26a4d 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -7,4 +7,5 @@ export { MosaicLayer } from "./mosaic-layer/mosaic-layer.js";
 export type { MosaicSource } from "./mosaic-layer/mosaic-tileset-2d.js";
 export type { MultiCOGLayerProps, MultiCOGSourceConfig, } from "./multi-cog-layer.js";
 export { MultiCOGLayer } from "./multi-cog-layer.js";
+export { inferRenderPipeline } from "./geotiff/render-pipeline.js";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index dbacdde78e52912c4ab7b76b2b8f5b3d79c1305f..fe7a27ad5300c18c1b71c57fa9e6c440bcffb211 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -3,4 +3,9 @@ export { addAlphaChannel } from "./geotiff/geotiff.js";
 export * as texture from "./geotiff/texture.js";
 export { MosaicLayer } from "./mosaic-layer/mosaic-layer.js";
 export { MultiCOGLayer } from "./multi-cog-layer.js";
+// objex patch: re-export inferRenderPipeline so consumers can build the
+// library-default uint pipeline and append modules (e.g. LinearRescale) without
+// forking the default getTileData and renderTile. Upstream does not expose this
+// helper yet. Track if upstream adds a `renderPipeline` prop to COGLayer.
+export { inferRenderPipeline } from "./geotiff/render-pipeline.js";
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/multi-cog-layer.js b/dist/multi-cog-layer.js
index 6d4cf5123c146837e87c8871994d6b167b702c56..9e57b3e67f2c8dc84ece87b83242d70a4e891cf8 100644
--- a/dist/multi-cog-layer.js
+++ b/dist/multi-cog-layer.js
@@ -503,17 +503,41 @@ function createBandTexture(device, array) {
     if (array.layout !== "pixel-interleaved") {
         throw new Error("Band-separate layout not yet supported in MultiCOGLayer");
     }
-    const { data, width, height } = array;
+    let { data, width, height } = array;
     let format;
     if (data instanceof Uint8Array || data instanceof Uint8ClampedArray) {
         format = "r8unorm";
     }
     else if (data instanceof Uint16Array) {
-        format = "r16unorm";
+        // objex patch: r16unorm requires EXT_texture_norm16, which Firefox on
+        // macOS does not enable on its WebGL2 context. Without the extension
+        // the texStorage call rejects internalformat 0x822A (GL_R16) and the
+        // texture is never specified, so MultiCOGLayer renders transparent
+        // tiles for every uint16 band (e.g. Sentinel-2 raw reflectance B*).
+        // When the device cannot use r16unorm, convert to a Float32Array
+        // (divided by 65535 to preserve r16unorm's normalized [0,1] sampling
+        // semantics) and upload as r32float instead. R32F is core in WebGL2
+        // and supported on every backend luma.gl ships. Linear filtering is
+        // available via OES_texture_float_linear, present in modern browsers;
+        // when absent the texture still samples with nearest, which is fine
+        // for raster mosaic display.
+        if (device.isTextureFormatSupported && !device.isTextureFormatSupported("r16unorm")) {
+            const f = new Float32Array(data.length);
+            for (let i = 0; i < data.length; i++)
+                f[i] = data[i] / 65535;
+            data = f;
+            format = "r32float";
+        }
+        else {
+            format = "r16unorm";
+        }
+    }
+    else if (data instanceof Float32Array) {
+        format = "r32float";
     }
     else {
         throw new Error(`Unsupported typed array type: ${data.constructor.name}. ` +
-            "Currently only Uint8Array and Uint16Array are supported.");
+            "Currently only Uint8Array, Uint16Array, and Float32Array are supported.");
     }
     return device.createTexture({
         data,

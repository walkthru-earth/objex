diff --git a/dist/raster-tileset/raster-tile-traversal.js b/dist/raster-tileset/raster-tile-traversal.js
index f3ce20a11f8e68bd57f61f786a5bc91311e6d996..f94a818b5c07ca0f4df3e9d7e4ca71518d5f907f 100644
--- a/dist/raster-tileset/raster-tile-traversal.js
+++ b/dist/raster-tileset/raster-tile-traversal.js
@@ -379,8 +379,13 @@ function sampleReferencePointsInEPSG3857(refPoints, tileBounds, projectTo3857) {
     for (const [relX, relY] of refPoints) {
         const geoX = minX + relX * (maxX - minX);
         const geoY = minY + relY * (maxY - minY);
-        // Reproject to Web Mercator (EPSG 3857)
         const projected = projectTo3857(geoX, geoY);
+        // Clamp non-finite results to valid Web Mercator range.
+        // Tile matrices can extend beyond the valid CRS domain (e.g. when a
+        // 2048px tile covers a 1171px overview, tile bounds reach lat=-177°).
+        // proj4 returns NaN for these out-of-domain coordinates.
+        if (!Number.isFinite(projected[0])) projected[0] = 0;
+        if (!Number.isFinite(projected[1])) projected[1] = geoY > 0 ? EPSG_3857_HALF_CIRCUMFERENCE : -EPSG_3857_HALF_CIRCUMFERENCE;
         refPointPositions.push(projected);
     }
     return refPointPositions;
@@ -502,8 +507,16 @@ export function getTileIndices(metadata, opts) {
     const { lowerLeft, upperRight } = wgs84Bounds;
     const [minLng, minLat] = lowerLeft;
     const [maxLng, maxLat] = upperRight;
+    console.log(`[DBG-TRAVERSE] getTileIndices wgs84Bounds: [${minLng},${minLat}] - [${maxLng},${maxLat}]`);
+    if (!Number.isFinite(minLng) || !Number.isFinite(minLat) || !Number.isFinite(maxLng) || !Number.isFinite(maxLat)) {
+        console.error('[DBG-TRAVERSE] NON-FINITE wgs84Bounds! This will cause "Invalid number null"');
+    }
     const bottomLeft = lngLatToWorld([minLng, minLat]);
     const topRight = lngLatToWorld([maxLng, maxLat]);
+    console.log(`[DBG-TRAVERSE] worldBounds: bottomLeft=[${bottomLeft}] topRight=[${topRight}]`);
+    if (!Number.isFinite(bottomLeft[0]) || !Number.isFinite(bottomLeft[1]) || !Number.isFinite(topRight[0]) || !Number.isFinite(topRight[1])) {
+        console.error('[DBG-TRAVERSE] NON-FINITE worldBounds from lngLatToWorld!');
+    }
     const bounds = [
         bottomLeft[0],
         bottomLeft[1],
diff --git a/dist/raster-tileset/raster-tileset-2d.js b/dist/raster-tileset/raster-tileset-2d.js
index 6b5fe5ec4b6ff55ef2ba861484e240d2d2bf61a5..145028e7f395550da6a4545e07467b6d649aee84 100644
--- a/dist/raster-tileset/raster-tileset-2d.js
+++ b/dist/raster-tileset/raster-tileset-2d.js
@@ -20,6 +20,7 @@ export class TileMatrixSetTileset extends Tileset2D {
     wgs84Bounds;
     projectTo3857;
     constructor(opts, tms, { projectTo4326, projectTo3857, }) {
+        console.log('[DBG-TILESET] constructor START, boundingBox:', JSON.stringify(tms.boundingBox));
         super(opts);
         this.tms = tms;
         this.projectTo3857 = projectTo3857;
@@ -29,6 +30,7 @@ export class TileMatrixSetTileset extends Tileset2D {
         this.wgs84Bounds = projectBoundsToWgs84(tms.boundingBox, projectTo4326, {
             densifyPts: 10,
         });
+        console.log('[DBG-TILESET] wgs84Bounds:', JSON.stringify(this.wgs84Bounds));
     }
     /**
      * Get tile indices visible in viewport

diff --git a/dist/index.js b/dist/index.js
index d4f9c22f5215a980da4a03f70e6cf551b3594f86..41eee5c78f95a26d4cddb3070a7947b83b514183 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -354,7 +354,8 @@ var _ZarrStore = class _ZarrStore {
       if (this.version === 3) {
         storeHandle = Promise.resolve(baseStore);
       } else {
-        storeHandle = zarr2.tryWithConsolidated(baseStore).catch(() => baseStore);
+        /* objex patch: zarrita 0.7 removed tryWithConsolidated; _loadV2 fetches .zmetadata itself */
+        storeHandle = Promise.resolve(baseStore);
       }
     } else {
       storeHandle = _ZarrStore._storeCache.get(storeCacheKey);
@@ -363,7 +364,8 @@ var _ZarrStore = class _ZarrStore {
         if (this.version === 3) {
           storeHandle = Promise.resolve(baseStore);
         } else {
-          storeHandle = zarr2.tryWithConsolidated(baseStore).catch(() => baseStore);
+          /* objex patch: zarrita 0.7 removed tryWithConsolidated; _loadV2 fetches .zmetadata itself */
+          storeHandle = Promise.resolve(baseStore);
         }
         _ZarrStore._storeCache.set(storeCacheKey, storeHandle);
       }
